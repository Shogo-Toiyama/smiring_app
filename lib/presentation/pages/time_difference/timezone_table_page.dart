import 'package:flutter/material.dart';

class TimezoneTablePage extends StatelessWidget {
  const TimezoneTablePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Timezone Table Page'),)
    );
  }
}